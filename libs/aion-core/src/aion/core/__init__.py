"""Aion Core — shared primitives for the Aion Python SDK.

Provides the A2A protocol layer, runtime context, agent invocation abstractions,
configuration, logging, HTTP utilities, and shared constants used across all
aion-* packages.
"""
