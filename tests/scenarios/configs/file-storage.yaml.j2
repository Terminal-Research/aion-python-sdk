# The default deployment with a file storage backend and the distribution
# extension enabled. The backend comes from the variant's environment
# (FILE_STORAGE_BACKEND); the extension is what lets a request name the
# organization a stored file belongs to.
{% include "default.yaml.j2" %}
