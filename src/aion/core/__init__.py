"""Aion Core — shared primitives for the Aion Python SDK.

Provides the A2A protocol layer, runtime context, agent invocation abstractions,
configuration, logging, HTTP utilities, and shared constants used across the
aion.* subpackages.

``aion.core.exceptions`` holds the root of the SDK's exception hierarchy,
``AionError``, and the exceptions callers of the SDK catch from their own code.
"""
